import {WebSidebarProviderProps} from "mmcore-ui";
import {mergeWind} from "../common/tailwind-utils";


export function DefaultSidebarProvider({className, ...props}: WebSidebarProviderProps) {
    return (
        <></>
    )
}