import {WebSidebarProps} from "mmcore-ui";
import {mergeWind} from "../common/tailwind-utils";


export function DefaultSidebar({className, ...props}: WebSidebarProps) {
    return (
        <></>
    )
}