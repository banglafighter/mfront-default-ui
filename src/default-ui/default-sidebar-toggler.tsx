import {WebSidebarTogglerProps} from "mmcore-ui";
import {mergeWind} from "../common/tailwind-utils";


export function DefaultSidebarToggler({className, ...props}: WebSidebarTogglerProps) {
    return (
        <></>
    )
}